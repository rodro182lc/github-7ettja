export interface TripEventColumnIcon {
  // X icon
  cross: boolean;
  // ⚠️(red)
  redExclamation: boolean;
  // Indicators for delayed/on-time status of Event
  dangerUnableCalc: boolean;
  danger: boolean;
  warning: boolean;
  success: boolean;
}
